apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: demo-app
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80
  type: {{ .Values.service.type }}
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: frontend-block-backend
  namespace: demo
spec:
  endpointSelector:
    matchLabels:
      app: nginx-frontend

  egress:
  - toEndpoints:
    - matchLabels:
        app: nginx-backend
